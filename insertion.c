#include<stdio.h>
int main(){
    int n,i,pos,temp;
    printf("enter no of elemnts");
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    printf("before insertion : ");
    for(i=0;i<n;i++){
        printf("%d ",arr[i]);
    }
    printf("khaa krana h insertion");
    scanf("%d",&pos);
    temp=n;
    
}
